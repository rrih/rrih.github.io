{
  "name": "rrih.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run build && bun scripts/serve-site.ts",
    "build": "bun scripts/build-site.ts",
    "preview": "bun scripts/serve-site.ts",
    "test": "bun scripts/verify-site.ts",
    "lint": "biome check scripts/*.ts",
    "dev-check": "bun run lint && bun run build && bun run test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@playwright/test": "1.55.1",
    "husky": "^9.1.7"
  }
}
